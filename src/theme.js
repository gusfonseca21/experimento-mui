import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: "#fff",
    secondary: {
      main: "#15c630",
    },
    otherColor: {
      main: "#999",
    },
  },
  background: {
    default: "#000",
  },
});
